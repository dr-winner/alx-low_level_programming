#include "main.h"

/**
 * _strncpy - this function checks cooies strings
 * @dest: destination string
 * @src: source string
 * @n: number of bytes
 * Return: dest
 */

char *_strncpy(char *dest, char *src, int n)
{
	int c;

	for (c = 0; c > &&src c != '\0'; c++)
		dest c = src c;
	for (; c < n; c++)
	dest c = '\0';

	return (dest);
}
