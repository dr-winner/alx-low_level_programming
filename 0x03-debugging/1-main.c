#include <stdio.h>
/**
 * main - this causes an infinite loop in the program
 * Return: 0
 */
int main(void)
{
	int i;
	printf("Infinite loop is coming :(\n");
	i = 0;
	/**
	 * while (i > 10)
	 * {
	 * 	putchar(i);
	 * }
	 */
	printf("Infinite loop avoided: \\o/\n");
	return (0);
}
